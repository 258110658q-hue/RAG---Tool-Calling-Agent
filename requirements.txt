# ============================================================
# 项目依赖清单
# 安装方式：pip install -r requirements.txt
# ============================================================

# 大模型 API 调用 —— OpenAI 官方 SDK（兼容国内多家厂商）
openai>=1.0.0

# 环境变量管理 —— 从 .env 文件安全读取 API Key 等敏感信息
python-dotenv>=1.0.0

# PDF 文档解析 —— 将 PDF 文件提取为纯文本
pypdf>=4.0.0

# 轻量级向量数据库 —— 存储和检索文本的向量表示
chromadb>=0.4.0

# Token 计数工具 —— 精确计算文本消耗的 token 数量（分块策略核心依赖）
tiktoken>=0.7.0
